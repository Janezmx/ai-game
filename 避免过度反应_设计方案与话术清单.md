# 「清醒边界」防止玩家过度反应的设计方案与话术清单

- 文档性质：**设计方案（不含任何代码改动）**，供评审后决定实施范围
- 版本：v1.0
- 日期：2026-09-15
- 适用范围：5 关（煤气灯操控 / 职场打压 / 亲情绑架 / 网络围攻 / 偏见伪装）的课前导读、对话对战、复盘报告、成长结算四个环节
- 关联文件（仅作取证引用，本次不修改）：
  - `shared/src/index.ts`（`LEVELS` / `KnowledgePoint` / `DimensionScores` / `NPCResponseAssessment` / `ALL_BADGES`）
  - `frontend/src/components/KnowledgeCard.tsx`（`LEVEL_KNOWLEDGE` 静态兜底文案）
  - `frontend/src/components/HeartDomainPrepareScreen.tsx`（课前导读渲染）
  - `frontend/src/components/BattleScreen.tsx`（明辨铃建议 chip、四维结算浮层、等待期知识卡）
  - `frontend/src/components/ReviewScreen.tsx`、`ReviewReportScreen.tsx`（复盘与存档详情）
  - `frontend/src/components/GrowthScreen.tsx`、`frontend/src/pages/HomePage.tsx`（成长与首页）
  - `frontend/src/utils/dimensions.ts`、`frontend/src/utils/reportExport.ts`（评分聚合与导出）
  - `backend/app/api/chat/route.ts`、`backend/app/api/insight/route.ts`、`backend/app/api/review/complete/route.ts`、`backend/app/api/npc/generate/route.ts`（提示词与结算）
  - `导出/复盘报告_第1关_煤气灯效应_20260910.md`（现有导出措辞基准）

---

## 摘要（一句话答案）

**把产品从"教识别"升级为"教辨别"**：给「识别信号」配上「不算操控的反例」，给「反击话术」配上「先确认的选项」，给「操控判定」配上「我也可能误判」的对照，给「四维评分」配上「辨别力/校准」这一维——玩家才不会把学到的框架当锤子，看谁都像钉子。

配套的三层防护：

| 层 | 解决的问题 | 主要手段 |
| --- | --- | --- |
| 教育层 | 只知道"什么是操控"，不知道"什么不是" | 课前卡新增「⚠️ 这些不算操控」「⏸ 先确认，再反击」两块对照 |
| 过程层 | 游戏机制奖励"越强硬越有效" | 明辨铃固定纳入温和确认型策略；对贴标签/读心/绝对化给出轻量实时反馈 |
| 结果层 | 复盘只有"他为什么操控你"，没有"我可能读错了" | 每轮增加「可能误判的情形」与「若对方是善意」脚本；结语增加现实迁移校准；成长/首页长期强化"边界不是对所有人都设防" |

---

## 一、问题诊断

### 1.1 现象：假阳性（false positive）

玩家通关后，把游戏里学到的"操控识别框架"套到全部人际沟通上，出现四类典型反应：

1. **对号入座**：正常的批评 → "他在打压我"；正常的冷淡 → "这是冷暴力/煤气灯"。
2. **读心定罪**：不确认对方意图，直接判断动机（"你就是故意让我怀疑自己"）。
3. **过度防御/攻击**：一被否定就贴标签、抬音量、拒绝沟通，把"守边界"做成"打回去"。
4. **关系性的代价**：对所有靠近的人先设防，亲密关系与协作关系同时受损。

用一句话概括：游戏教了"如何识别操控"，却没有教"**什么时候不该用这个框架**"，玩家手里只有一把锤子。

### 1.2 心理机制：为什么学完反而更容易过度反应

| 机制 | 说明 | 在本游戏中的体现 |
| --- | --- | --- |
| 可得性偏差 | 刚学会的概念最容易被调用 | 5 关全是"操控"样本，大脑里没有"非操控"对照组 |
| 确认偏误 | 有了框架就只找支持框架的证据 | 复盘每轮都在告诉玩家"NPC 的动机是什么"，等于每轮都在确认"对方有问题" |
| 威胁敏感化 | 反复演练防御会提高警戒基线 | 战败/护盾掉血带来真实压力，"保持警惕"被反复强化 |
| 归因偏误 | 把对方行为归因于人格，而非情境 | 目标叙事固定为"NPC 是操控者"，缺"他可能只是累了/急了"的解释 |
| 技能泛化 | 高强度训练的技能会越界使用 | 游戏奖励"坚定回应"，玩家学到"越硬越有效" |

### 1.3 现有设计中助长过度反应的具体设计点（代码取证）

> 以下每条都已在实际代码/文案中确认，是本次方案要修的对象。

**① 知识点卡只有"信号"，没有"反例"**

`frontend/src/components/KnowledgeCard.tsx` 中 `LEVEL_KNOWLEDGE` 每关的结构固定为 `definition + signals[]（🔍 识别信号）+ healthyResponse[]（💬 健康应对）+ caseStory（📝 真实案例）`。全部 5 关的 `signals` 都是单向的"危险信号"，**没有任何一条告诉玩家"这不是操控"**。以第 1 关为例：

- 「对方常说『你太敏感了』『你记错了』」
- 「你的真实感受被说成是『想太多』」

玩家学到的是一张"筛子"，但没有学到筛子的孔径边界。

**② 明辨铃提示词只要求"反击"，不要求"确认"**

`backend/app/api/insight/route.ts` 的 `INSIGHT_PROMPT` 原文要求：

> "每条建议必须做到：1. 能有效反击本轮操控：或直接点破对方话里的逻辑漏洞/偷换概念，或指出对方强加在自己身上的预设，或冷静重申事实与自己的真实感受，**或干脆不接对方抛来的'自证/内疚'包袱**"
> "三条必须策略错开，例如：A. 直接戳破歪曲、重申事实；B. 温和而明确地划出边界…；C. 反问其逻辑漏洞或拒绝接受莫须有的指责。"

三个策略全是"对抗/拆解"类，**没有一条是"先求证对方意图"**。玩家每轮点选建议，就是在反复练习"先定性、后回应"。

**③ 评估口径明确让"坚定/强硬"主导结算**

`backend/app/api/chat/route.ts` 的 `ASSESSMENT_INSTRUCTION_TEMPLATE` 原文：

> "- dimensions 沿用旧逻辑：**坚定回应（assertiveResponse）主导控制力结算**，其余维度影响护盾/迷雾"

配套结算公式（同文件第 1230 行起）：

```1230:1235:backend/app/api/chat/route.ts
          let dim = assessment.dimensions;
          if (dim) {
            const resistDelta = 10 - dim.boundaryAwareness / 10;
            // 单轮 NPC 操控变化幅度钳位到 ±15，防止 LLM 输出异常评分（如边界值/缺失）时出现跳变，
            // 导致玩家一句话就把 NPC 操控打归零而提前胜利。
            const rawControlDelta = (50 - dim.assertiveResponse) / 5;
```

`assertiveResponse` 越高，`npcControlLevel` 掉得越快 → **越强硬越快赢**。四维里没有任何一维会因为"读心、贴标签、拒绝沟通"而扣分。

**④ 胜利条件是"连续 2 轮 effective + 控制力 < 30"**

```1211:1214:backend/app/api/chat/route.ts
          const isEffective = (assessment.playerStatus === "effective");
          session.effectiveStreak = isEffective ? session.effectiveStreak + 1 : 0;
          // 仅当连续 2 轮 effective 且当前控制 < 30 才视为 NPC 认输
          const npcSurrender = session.effectiveStreak >= 2 && session.npcControlLevel < 30;
```

而 `playerStatus` 只有 `effective | shaken | trapped` 三态，**没有"过度反应"这一态**。也就是说，玩家用攻击性语言把 NPC 打退，系统同样判定为"有效防御"。

**⑤ 徽章奖励把分数拉满**

`shared/src/index.ts` 的 `ALL_BADGES`：

> `{ id: "perfect_defense", name: "完美防御", description: "任意一关综合评分≥90", icon: "✨" }`

`frontend/src/pages/GamePage.tsx` 中 `if (avgScore >= 90) state.unlockBadge("perfect_defense");`。

综合分 = 四维均值（`frontend/src/utils/dimensions.ts` 的 `averageOf`），其中含"坚定回应"。**"完美防御"在客观上奖励了把坚定/强硬拉满**，隐含"越硬=越完美"的价值判断。

**⑥ 复盘文案是"判定式"的，直接给 NPC 定性**

复盘逐轮渲染 `🎯 NPC 的动机`（`backend/app/api/review/complete/route.ts` 生成的 `trapAnalysis`），导出示例可见其措辞：

> **🎯 NPC 的动机**
> 用"你记混了""我从来没碰过"直接否定对方记忆，**诱导玩家怀疑自己的认知**。

> **🔍 本轮科普点评**
> 这句在暗示"**问题出在你的脑子，不在我**"，你会被诱导去反复查证记忆、自证清白。

这类文本没错，但全是"对方的恶意 + 我的被动"，**没有一处写"这句话也可能只是……"**。玩家读 5 关 × 5 轮的复盘，等于被强化 25 次"别人在操控我"的世界观。

另外，`ReviewScreen.tsx` 的 `getAdvice` 在非有效状态下输出"保持警惕，注意识别操控手法"，在有效状态下输出"继续保持这种状态"——只有"更警惕"和"保持警惕"两种取向。

**⑦ 全站只有"识别/抵御"叙事，没有"辨别"叙事**

- 首页（`frontend/src/pages/HomePage.tsx`）："守护你的心域，**识别并抵御**无形操控"；"学会识别煤气灯操控、职场打压、亲情绑架等操控手法"。
- 复盘页（`ReviewScreen.tsx`）："回顾你的应对，**识别操控套路**"。
- 备战页（`HeartDomainPrepareScreen.tsx`）："📚 课前导读 · 这一关要学会**识别**"。

游戏从入口到出口，从未提示过"大多数人的大多数话，并不是操控"。

**⑧ 结尾没有任何"现实迁移"提示**

复盘最后一屏是"🔄 更好的回应方式 + 本轮四维"，导出 md 结尾是"— 共 N 轮对话 · 由「清醒边界」自动存档生成"。**没有任何一处提示玩家：这是浓缩的极端情境，不要把这里的话术直接搬去对待身边的人。**

### 1.4 反向风险（必须同时防住）

如果只做"降温"，会从"怀疑一切"滑向"必须忍让、必须示弱"，同样是错的。需要明确写进设计约束：

- 「先确认」只能是**并列选项**，不能是唯一正解；复盘必须同时给出"这确实是操控时该怎么办"。
- 不允许把"道歉/讨好/自我否定"当作高分回应。
- 不允许把"误伤"变成纯粹的分数惩罚——那会教出"少说话最安全"的回避型玩家。

---

## 二、设计原则

### P1 教辨别，不只教识别

每个"危险信号"必须能对应到一个"安全反例"。只给信号是训练筛子，给出反例才是训练辨别力。

### P2 区分"行为"与"人格"

所有文案与评分都指向**这句话的作用**，而不是**对方是什么人**。
- ❌ "NPC 的动机是瓦解你的自信" → ✅ "这句话的作用是让你开始怀疑自己的记忆"
- ❌ "你这种人就是…" → ✅ "你刚才那句话把'这件事'换成了'你这个人'"

### P3 给"非对抗出口"

每轮至少提供一个"先确认/陈述感受"的低对抗选项。让玩家知道：**不接战也有路可走**，而不是只有"忍"和"打"两个选项。

### P4 把自我校准做成可见反馈

"我可能误判了"必须是一个能被看见、被记住、被奖励的东西（校准分、复盘对照栏、校準徽章），而不是一句藏在文末的免责声明。

### P5 代价落在"信息"而非"分数"

误伤的真实代价是"错过一次澄清、伤到一段关系"，而不是"扣 20 分"。机制设计上，误判主要影响"校准"反馈，不重罚四维与护盾——否则玩家学到的不是"更谨慎"，而是"更回避"。

### P6 保持术语一致性

全站统一使用「辨别力 / 校准」一词（英文标识建议 `calibration`），避免出现"同理心""过度敏感""玻璃心"等带价值判断的替代说法。

---

## 三、四环节改动设计（教育层）

### 3.0 总览

| # | 环节 | 加在哪里 | 改什么 | 为什么有效 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| 1 | 课前导读卡 | `KnowledgeCard.tsx` 的卡片结构 | 新增「⚠️ 这些不算操控」与「⏸ 先确认，再反击」两块，与「🔍 识别信号」成对呈现 | 在玩家建立框架的第一分钟就给出孔径边界 | P0 |
| 2 | 对话对战 | `backend/app/api/insight/route.ts` 的 `INSIGHT_PROMPT` + `BattleScreen.tsx` 建议区 | 三条建议固定含 1 条"先确认/陈述感受"型；对贴标签/读心/绝对化给轻量实时提示 | 在被操控感最强的时刻提供非对抗选项，打断"先定性"的条件反射 | P0/P1 |
| 3 | 复盘报告 | `ReviewScreen.tsx` 每轮卡片 + 报告结语 | 每轮增加「🔍 这一轮可能是我误判」；结语增加「🌱 现实迁移校准」+ 三问自检卡 | 把"我可能错了"变成可回看的内容，抵消"别人都在操控我"的单向强化 | P0/P1 |
| 4 | 成长/结算 | `GrowthScreen.tsx`、`HomePage.tsx`、`ALL_BADGES` | 雷达图下加"边界不等于强硬"注解；新增校准类徽章与提示语 | 长期反复强化，抵消"分越高越是好沟通者"的暗示 | P0/P2 |

### 3.1 课前导读卡

**加在哪里**：`frontend/src/components/KnowledgeCard.tsx`，在「🔍 识别信号」之后、「💬 健康应对」之前插入两块。

**结构改动**：`KnowledgePoint`（`shared/src/index.ts`）新增两个可选字段（保持向后兼容，旧存档无字段时这两块不渲染）：

```ts
/** 「这些不算操控」：与 signals 一一对照的安全反例 */
nonSignals?: string[];
/** 「先确认，再反击」：证据不足时的求证话术 */
confirmationPrompts?: string[];
```

若希望连类型都不动，也可把这两块文案临时并入 `healthyResponse`（不推荐：会让"健康应对"变成混杂的清单，语义丢失）。

**渲染形态**（沿用现有 `sectionLabel` 样式，emoji 与现有卡片一致）：

```
🔍 识别信号
• 对方常说「你太敏感了」「你记错了」
• 你的真实感受被说成是「想太多」

⚠️ 这些不算操控
• 对方承认「这次是我记错了」，并愿意一起核对记录
• 对方在你表达不满后，问的是「你希望我以后怎么做」

⏸ 先确认，再反击
✓ 我想先确认一下：你是不记得那件事，还是我说得不够清楚？
✓ 如果是我记错了，我愿意改；如果是我们记的不一样，那就把事实摆出来看。

💬 健康应对
✓ ……
```

**为什么有效**：现有卡片的结构是"信号 → 应对话术 → 案例"，玩家形成的映射是"看到信号 X → 用话术 Y 反击"。插入反例后，映射变成"看到类似的话 → 先判断它落在哪一侧 → 再决定要不要设防"，这正是辨别力的定义。

**全部 5 关的具体文案**见「四、话术清单」4.1 / 4.2。

### 3.2 对话对战过程

**（a）明辨铃话术池纳入"温和确认型"**

改 `backend/app/api/insight/route.ts` 的 `INSIGHT_PROMPT`：

1. 把策略分类从现有的 A/B/C（全部对抗型）改为四类：
   - **A. 先确认**（求证意图 / 索要具体例子）——新增，必出 1 条
   - **B. 陈述感受 + 重申事实**（"我不太舒服，因为刚才……"）
   - **C. 就事论事定边界**（"可以改，但我不接受这种方式"）
   - **D. 拒绝接盘**（不接自证/内疚包袱）——保留
2. 硬约束："**3 条中至少 1 条为 A 类，且 A 类不得包含任何对对方动机的判断**"。
3. 反向约束（防跑偏）："A 类可以温和，但**不得是道歉、讨好或自我否定**；确认之后仍要表达自己的立场，否则不算有效回应。"
4. 给 2-3 条 A 类 few-shot 示例（见 4.3），并说明"这几条的共同点是：只问事实与需求，不猜对方意图"。

**前端**：`BattleScreen.tsx` 的建议 chip 区不需要改结构，但建议给 A 类建议加一个极轻的标签（如"🧭 先确认"），让玩家在点选前就知道这是"不接战"的一条路。成本极低，教育价值很高。

**为什么有效**：现有玩家行为分布受"可用选项"支配——屏幕上有三条反击话术，玩家就会选反击。把"先确认"变成每轮都出现的一个选项，才有可能形成新的行为习惯；而"确认之后仍要表明立场"的约束，避免了把它做成示弱训练。

**（b）过度攻击的轻量实时反馈**

- 触发条件（任一）：贴标签（"你就是个…""你这种人"）、读心定罪（"你根本就是想…"）、绝对化（"你从来没…""你永远…"）、关门拒谈（"没什么好说的""随便你"）。
- 呈现方式：不做拦截、不扣护盾、不影响回合推进，只在结算浮层上方或输入框上方出现一条一次性提示（3 秒淡出，或随结算一起展示）。
- 判定来源：优先用评估模型新增的 `overreaction` 字段（P1）；模型不可用时用前端关键词规则兜底（降级路径，不阻塞）。
- 全部 6 条提示语见 4.5。

**为什么不直接扣分**：直接扣分会把"表达激烈"与"误伤对方"混为一谈（人在被侵害时提高音量是合理的）。只提示、只记录，让玩家自己在复盘里看到"我当时把它当成了操控，其实不一定"。

### 3.3 复盘报告

**（a）每轮增加「🔍 这一轮可能是我误判」**

位置：`frontend/src/components/ReviewScreen.tsx` 每轮卡片内，紧跟在「🎯 NPC 的动机」之后、「🧘 你的回应」之前（先给判断，再给对照）。

同时把「🎯 NPC 的动机」这个标题改为 **「🎯 这句话的作用」**（`ReviewScreen.tsx` 与 `reportExport.ts` 的 `**🎯 NPC 的动机**` 两处），把 `trapAnalysis` 的产出要求从"点破动机"改成"说明这句话在对话里起的作用"。这是 P2 原则的落地：**描述行为，回避人格定性**。

文案形态：

```
🔍 这一轮可能是我误判
如果对方当时的意思是「我确实不记得了」，那么这句话也可以读成一次记不清，
而不是在否认我的记忆。
（判断依据：一两次语言 ≠ 煤气灯，关键看它是否反复出现、是否同时否定了我表达的权利。）
```

**（b）每轮增加「🌱 若对方是善意」**

位置：紧跟在「🔄 更好的回应方式」之后，作为并列的第二组脚本（不要让"更好的回应"变成唯一答案）。

```
🌱 若对方是善意，可以这样接
1. 如果是你记不清了，那我们对着记录看一下；如果不是，我们就说这件事。
2. 我刚才那句语气不好，我承认；现在我想回到项链这件事本身。
```

**（c）报告结语增加「🌱 把练习带回现实」**

位置：`ReviewScreen.tsx` 的 `ScrollView` 末尾（rounds 之后）、导出 md 的结尾。内容见 4.8，含三个自检问题卡。

**（d）总结评语去攻击性化**

`ReviewScreen.tsx` 的结论文案目前是"你非常擅长识别心理操控，边界意识很强"——保留，但补一句校准提醒；并在 `getAdvice` 的 `default` 分支把"保持警惕，注意识别操控手法"改为"留意是不是操控，也留意这一次可能只是普通的情绪"。

**为什么有效**：玩家对复盘内容的记忆远强于对局中的即时击杀感。复盘里每轮出现一次"我也可能读错"，等于给 25 轮"他在操控我"的判定做了等量的对冲。

### 3.4 成长/结算页

**（a）雷达图注解**

`frontend/src/components/GrowthScreen.tsx` 的「📊 四维能力雷达」下方（`legendHint` 附近）增加一句：

> 坚定回应不是越硬越好。清晰的边界，通常听起来是平静的。

**（b）新增校准类徽章**（P2）

在 `shared/src/index.ts` 的 `ALL_BADGES` 增加：

| id | name | description | icon |
| --- | --- | --- | --- |
| `calibrator` | 校准者 | 在一局中面对"难听的真话"没有误伤对方 | 🧭 |
| `gentle_boundary` | 温和的边界 | 用"先确认"化解过一次可能的误伤 | 🕊️ |

同时**修正 `perfect_defense` 的语义**：把"任意一关综合评分≥90"的表述改为强调"高强度练习下的稳定发挥"，并在描述里补一句"高分代表防御技巧熟练，不代表现实中要如此对待每个人"。它现在的问题是暗示"分越高=越会沟通"。

**（c）首页叙事补一句**

`HomePage.tsx` 的 `descCard` 内补一行（放在现有描述之后）：

> 这里练习的是「辨别」，不是「怀疑」——现实里，多数人说错话的时候并没有恶意。

**（d）结算页（战败/胜利弹窗）**

`BattleScreen.tsx` 的胜利/失败弹窗各加一句校准提示，文案见 4.9。胜利时最容易强化"我赢了=我是对的"，这一句必须加。

---

---

## 四、话术清单（可直接使用）

### 4.1 课前卡新增栏目一：「⚠️ 这些不算操控」（5 关）

> 用途：`KnowledgePoint.nonSignals`，与同关 `signals` 逐条对照。
> 写法要求：每条都必须**具体到可观察的一句话/一个动作**，禁止"尊重你""理解你"这类抽象词。

#### 第 1 关 · 煤气灯操控（亲密关系）

1. 对方承认「这次是我记错了」，并愿意一起翻聊天记录或其他记录
2. 对方说「我当时是这么想的」，而你们确实只是理解不同，他也承认自己讲得不清楚
3. 对方解释迟到是因为睡过头，并给出下次的具体做法，没有反过来指责你太计较
4. 对方在你表达不满后，问的是「你希望我以后怎么做」，而不是「你怎么又敏感了」

#### 第 2 关 · 职场打压

1. 上司指出方案的具体问题（哪一页、哪个数据、哪句结论），并说明可以怎么改
2. 评价只针对这次的产出，没有顺带评价你这个人「行不行」
3. 明确说明晋升落选是因为哪一项没达标，并给出时间和资源让你补
4. 你提出异议后，他没有因此改变对你的态度，也没有暗示你「态度有问题」

#### 第 3 关 · 亲情绑架

1. 父母表达想念和期待，但接受你今年不回家的决定
2. 明确说「我希望你考公，但这是你的选择」，没有把选择与孝不孝顺挂钩
3. 提出请求时说明「不方便也没关系」，你拒绝后不再追着问原因
4. 你说了难处之后，讨论的是折中方案，而不是反复强调过去为你牺牲了多少

#### 第 4 关 · 网络围攻

1. 有人就事论事地反对你的观点，不给你贴身份标签，也不号召别人一起上
2. 有人指出你上一条表达得不够清楚，并说明他理解成了什么
3. 批评针对具体内容，不针对你的人格、外貌或私生活
4. 你说「这是我理解错了」之后，对方就停下了，没有继续追打

#### 第 5 关 · 偏见伪装

1. 面试官说明这个岗位的硬性要求（年限、技能、语言），并明确你不符合哪一条
2. 对方指出的是具体不足（某项技能不熟），而不是含糊地说「怕你适应不了」
3. 决定理由透明：标准是什么、你排在哪、差多远，都说得出来
4. 你问「这个标准对所有人一致吗」之后，对方愿意回答或修正，而不是觉得你太敏感

### 4.2 课前卡新增栏目二：「⏸ 先确认，再反击」（5 关）

> 用途：`KnowledgePoint.confirmationPrompts`。
> 三步结构统一为：**① 先要具体** → **② 再说感受** → **③ 最后定边界**；每条话术都必须是玩家能原样说出口的第一人称原话，不得出现"你可以…""建议…"。

#### 通用三步（放在卡片顶部作为小字说明）

> 先要具体（哪件事、哪句话）→ 再说感受（我听到的是…我有点困惑）→ 最后定边界（如果是…，我希望…）。

#### 第 1 关 · 煤气灯操控

- 我想先确认一下：你是不记得那件事，还是我说得不够清楚？我们对着记录看一下。
- 我刚才有点被绕晕了。你先告诉我，这件事你记得的版本是什么。
- 如果是我记错了，我愿意改；如果是我们记的不一样，那就把事实摆出来看。

#### 第 2 关 · 职场打压

- 我想确认一下：你说的是这次方案的哪一部分？具体哪一页，我照着改。
- 这类评价我听得进去，但我想知道评判的标准是什么，我好对着补。
- 我可以接受这次不晋升。你能告诉我差在哪一项吗？我按这个来。

#### 第 3 关 · 亲情绑架

- 我知道你们是担心我。我们先说清楚：这件事你们希望我做到哪一步？
- 我理解你的心情，但我需要先确认——如果我这次真的不回去，你们会怎么想？
- 我不是不愿意帮，我想先弄清楚这笔钱具体用在哪，我们再决定怎么给。

#### 第 4 关 · 网络围攻

- 我先确认一下你说的是哪一条内容，我把上下文补全，你再判断。
- 你可以不同意我的观点，但我想知道你是针对这句话，还是针对我这个人。
- 我愿意改表达，但我不接受这种骂法。我们把具体那一句拿出来说。

#### 第 5 关 · 偏见伪装

- 我想先确认，这个岗位的硬性要求是哪几条？我看看我差在哪。
- 如果标准是对所有人一样的，请问我这一项和别人的差距具体是什么？
- 我不是拒绝被评估，我只是想知道评估用的是什么标准。

### 4.3 明辨铃「温和确认型」话术（12 条，可直接发送）

> 用途：写入 `INSIGHT_PROMPT` 作为 A 类策略的 few-shot 示例，或作为模型失效时的本地兜底建议池。
> 硬约束（与现有 `cleanAlternativeText` 保持一致）：第一人称口语原话、10-40 字、无"你可以/建议/试着"等引导词、无动作或表情描写、无第二人称替玩家说话。
> 附加约束：**不得是道歉讨好**（如"对不起是我太敏感了"），也不得只停在确认（只问不表立场，评分上不算有效）。

| # | 话术（可直接发送） | rationale（为什么有效） |
| --- | --- | --- |
| 1 | 我想先确认你的意思，是这件事我没做好，还是你觉得我不上心？ | 把模糊的否定拆成两个可回答的具体问题，避免自动进入辩护或反击。 |
| 2 | 你说的是哪一句话？把具体那句说出来，我们再讨论。 | 拒绝笼统否定，把对话拉回可验证的事实。 |
| 3 | 我听明白了，但我需要具体一点的信息，才知道该怎么改。 | 承认信息、索要标准，不接情绪、不接人格评价。 |
| 4 | 我可以听你的意见，但我不接受用这种方式说我。 | 把"内容"和"方式"分开，守住边界而不否定对方整个人。 |
| 5 | 我有点困惑，你说的这件事和我记得的不一样，我们对一下细节。 | 用困惑替代指控，把记忆冲突交给事实而不是情绪。 |
| 6 | 你的提醒我收到了，我先想想，想清楚再回你。 | 给情绪降温、给自己时间，避免在激动时把话说死。 |
| 7 | 这件事我可以改，但我不想被说成是态度有问题。 | 接住可改进的部分，推开人格指控。 |
| 8 | 我理解你是为我好，但这句话让我不太舒服，我想说出来。 | 先接善意、再表达感受，既不通敌也不对抗。 |
| 9 | 我想先分清，你是在给我建议，还是在要求我必须这样做？ | 让"建议"与"命令"分开，边界自然浮现。 |
| 10 | 你可以继续说，但请先说明你想让我做什么，我才知道怎么回应。 | 把对话导向需求而不是指责，减少无谓对抗。 |
| 11 | 我可能理解错了，你先说说你的本意，我再决定怎么接。 | 承认可能误判，同时不做无原则的自我否定。 |
| 12 | 如果只是我们看法不同，那没关系；如果是你说错了，我希望你能承认。 | 给双方留台阶，也保留要求对方负责的权利。 |

**校验说明**：以上 12 条均通过现有 `cleanAlternativeText` 的清洗规则（不以"可以/建议/试着/尝试/不妨/比如/例如/坚定…地/冷静，"开头；含"我"或"你"；长度 ≥ 3 且 ≥ 10 字时含人称代词），可直接作为兜底池使用。

**策略配比建议**（写进 `INSIGHT_PROMPT`）：

```
三条建议必须同时覆盖以下两类，且至少 1 条属于 A 类：
- A 类「先确认」：只问事实与需求，不判断对方动机，不读心、不贴标签。
- B/C/D 类「划边界 / 重申事实 / 拒绝接盘」：可以坚定，但不得攻击对方这个人。
禁止输出：道歉讨好、自我否定、“你这种人/你根本就是/你从来没”等定性句式。
```

### 4.4 明辨铃的三条策略标签（前端展示用）

`BattleScreen.tsx` 的建议 chip 可在文字前加一个极轻的类别标签，让玩家在点选前就能看到"非对抗路径"的存在：

| 标签 | 用于 | 备注 |
| --- | --- | --- |
| 🧭 先确认 | A 类 | 建议每轮至少有 1 条 |
| 🛡️ 划边界 | B/C/D 类 | 保留现有风格 |
| 🔍 说事实 | B 类（重申事实） | 与"划边界"区分开，强调只讲事实不评判 |

### 4.5 对战过程「过度攻击」实时反馈提示语（6 条）

> 触发条件（任一即触发，不阻断流程、不扣分、3 秒淡出或随结算展示）：
> ① 贴标签（你就是个…、你这种人）② 读心定罪（你根本就是想…、你就是故意）③ 绝对化（你从来没…、你永远…）④ 关门拒谈（没什么好说的、随便你）⑤ 反羞辱（你自己不也一样）。

1. 你在守住边界，还是在反击对方这个人？先分开这两件事。
2. 这句话把「这件事」换成了「你这个人」。要不要试着只说这件事？
3. 「你从来/你永远」很难被证明，也很难被回应。换成"这一次"会更有力。
4. 你不想聊了可以暂停，但直接关门会让边界变成一堵墙。
5. 如果对方当时并没有恶意，这句话会让你错过一次澄清的机会。
6. 强硬不等于清晰。把要求说具体，通常比提高音量更管用。

> 触发用词本身不带贬义（避免"你又过度反应了"这类指责式提示），只描述**这句话做了什么**（把事换成了人）。

### 4.6 复盘「🔍 这一轮可能是我误判」模板与示例

> 位置：`ReviewScreen.tsx` 每轮卡片内，「🎯 这句话的作用」之后、「🧘 你的回应」之前。
> 句式模板：**如果对方当时的意思是____，那么这句话也可以读成____。判断依据：____。**

#### 第 1 关 · 煤气灯操控

- 如果对方那天确实不在场，那么「我从来没碰过」也可能只是一句陈述，而不是在否认你的记忆。
  判断依据：一两次语言 ≠ 煤气灯，关键看它是否**反复出现**、是否同时**否定了你表达的权利**。
- 如果对方只是累了、语气平淡，那么「你又在钻牛角尖」也可能只是不耐烦，而不是有计划地让你怀疑自己。
  判断依据：看他是否在你说清楚之后**愿意回到事实**。

#### 第 2 关 · 职场打压

- 如果「你这个方案还不行」后面确实跟了具体意见，那这是一次粗糙但真实的反馈。
  判断依据：批评是否**指向产出**，是否**给了改进路径**。
- 如果组里其他人也被同样严厉地对待，那是管理风格问题，不一定是针对你的打压。
  判断依据：标准是否**只对你一人**加严。

#### 第 3 关 · 亲情绑架

- 如果父母只是表达想念、没有用愧疚施压，那是期待，不是绑架。
  判断依据：**不接受拒绝**才是绑架，**表达期待**不是。
- 如果在你说明难处后他们接受了折中方案，那这次沟通其实是成功的。
  判断依据：看他是否**允许你有第二个答案**。

#### 第 4 关 · 网络围攻

- 如果对方只是就事论事地反对你的观点，那是分歧，不是网暴。
  判断依据：看**是否针对人**、**是否靠人数压制**、**是否伪造证据**。
- 如果批评你的人愿意把话说具体，那这次对话还有空间。
  判断依据：**越具体越可能是真意见，越笼统越可能是发泄。**

#### 第 5 关 · 偏见伪装

- 如果对方提出的要求对所有人一致，那是标准高，不是歧视。
  判断依据：拿**同一把尺子**量一遍，看刻度是否相同。
- 如果对方在你说出顾虑后调整了说法，那次可能只是无意识的措辞。
  判断依据：看他是**修正标准**，还是**要求你额外证明自己**。

### 4.7 复盘「🌱 若对方是善意」替代回应（每关 2-3 条）

> 位置：紧跟在「🔄 更好的回应方式」之后，与它并列（不是替代），标题必须在语义上明确"这是另一种可能"。

#### 第 1 关 · 煤气灯操控

1. 如果是你记不清了，那我们对着记录看一下；如果不是，我们就说这件事。
2. 我刚才那句语气不好，我承认；现在我想回到项链这件事本身。
3. 我想知道你怎么看这件事，而不是你怎么看我。

#### 第 2 关 · 职场打压

1. 你刚才指出的问题我记下来了，具体到哪一页我会改。
2. 我想听听你觉得可以怎么改，也想知道你评判的标准。
3. 这次我没做好，我认；但哪些部分是我独立完成的，我想说清楚。

#### 第 3 关 · 亲情绑架

1. 我知道你们想我，今年我确实回不去，我们想想别的办法见面。
2. 我很感谢你们为我做的这些，但这件事我想按自己的方式试一次。
3. 你们担心的话，我们把最坏的情况写下来一起看，而不是先劝我放弃。

#### 第 4 关 · 网络围攻

1. 我理解你不同意我的观点，我们把分歧说清楚就好，不用互相贴标签。
2. 我上一条说得不够准确，我补全一下，你可以再判断。
3. 我不想对线，但我保留我的说法。我们各自说完就到此为止。

#### 第 5 关 · 偏见伪装

1. 我想知道你判断我不合适的依据，如果它是标准，那我接受并去补。
2. 你提醒的这点我会注意；也想请你用同样的标准看看其他人。
3. 我愿意证明自己，但希望是按这个岗位真正的标准。

### 4.8 复盘结语：「🌱 把练习带回现实」（按关定制）

> 位置：`ReviewScreen.tsx` 的 rounds 之后、`reportExport.ts` 的页脚之前。
> **按关卡定制**：引言、「⚠️ 本关最容易被误读成操控的」与三个问题都随关卡变化；
> 栏目名「🧭 三个问题，帮你分辨」、两种结果分支与收尾句跨关共用。
> 单一来源：`frontend/src/utils/realityTransfer.ts` 的 `getRealityTransfer(level)`，
> 复盘页面与导出的 md 都从这里取，不允许各自硬编码。

**通用骨架（跨关不变的部分用花括号标出变动位）**

```
🌱 把练习带回现实
{本关引言}

⚠️ 本关最容易被误读成操控的
{本关最易误读的情境 + 它与操控的判据}

🧭 三个问题，帮你分辨
1. {模式性：一贯 vs 这一次}
2. {表达与选择权：针对事 vs 针对人}
3. {表态后的反应：给标准、共同核对 vs 继续否定、让你自责}

三个都是「否」：这可能只是一次不愉快的沟通。先试着确认，再决定要不要设防。
三个都是「是」：那值得认真设边界。该说的说清楚，该远离的远离，必要时寻求身边人或专业帮助。

边界不是对所有人都设防。
它只是让你更清楚：谁可以走近一点。
```

**硬约束**：三个问题必须落在同一三段式骨架上（① 模式性 ② 表达与选择权 ③ 表态后的反应），**且总数恒为 3 条**——下方两种结果分支的文案是"三个都是「否」/「三个都是「是」"，增删条目会让分支失效。若每关问题的结构也各不相同，这一节就会从"可迁移的判断方法"退化成"每关多背三条知识"。

**5 关定稿文案**

**第 1 关 · 煤气灯操控**

- 引言：这一关练的是「记忆被否认」。现实里，人是真的会记不清。
- 最易误读：对方说「我不记得了」，可能是煤气灯的开端，也可能只是真的忘了——区别在于他是否允许你去核对。
- 三个问题：
  1. 这是他一贯的说话方式，还是只有这一次？
  2. 是你不许查证、必须相信他，还是你们可以对着记录一起看？
  3. 你说了「我不太确定」之后，他是帮你把事实理清，还是让你更怀疑自己？

**第 2 关 · 职场打压**

- 引言：这一关练的是「被持续贬低」。现实里，批评本身并不等于打压。
- 最易误读：一句粗鲁的否定，可能是在打压你，也可能是一次糟糕但真实的反馈——区别在于批评后面有没有可执行的方向。
- 三个问题：
  1. 这是每次汇报都出现，还是只在这件确实没做好的事上？
  2. 他说的是这份产出，还是在说你这个人「行不行」？
  3. 你提出具体疑问之后，他是给了标准，还是换了个说法继续否定？

**第 3 关 · 亲情绑架**

- 引言：这一关练的是「用愧疚换服从」。现实里，家人表达期待是正常的。
- 最易误读：「我希望你回来」，可能是在绑架你，也可能只是想念——区别在于你说「不」之后，他是否接受。
- 三个问题：
  1. 这是每次沟通都绕回来的话题，还是偶尔提一次？
  2. 他们是在说自己的期待，还是要求你放弃自己的选择？
  3. 你说出难处之后，他们是在找折中办法，还是让你觉得不答应就是不孝？

**第 4 关 · 网络围攻**

- 引言：这一关练的是「被一群人围攻」。现实里，多数反对只是普通分歧。
- 最易误读：一条尖锐的反驳，可能是网暴，也可能只是观点冲突——区别在于他针对的是你的话，还是你这个人。
- 三个问题：
  1. 这是很多人同时涌上来，还是只有一两个人不同意你？
  2. 他是在反驳你的观点，还是在给你贴标签、贴身份？
  3. 你说「我理解错了」之后，他是停下，还是继续追着打？

**第 5 关 · 偏见伪装**

- 引言：这一关练的是「以善意包装的预设」。现实里，严格的筛选也是常态。
- 最易误读：一次追问或质疑，可能是微侵犯，也可能只是标准高——区别在于同一把尺子是否也量了别人。
- 三个问题：
  1. 这是对所有人都一致的要求，还是只对你额外加了一条？
  2. 他是在说你的能力不符，还是在说你的身份「不太合适」？
  3. 你问「标准是什么」之后，他愿意说明，还是觉得你太敏感？

### 4.9 成长 / 结算 / 首页长期提示语

| 位置 | 文案 |
| --- | --- |
| 首页 `descCard` 补充行 | 这里练习的是「辨别」，不是「怀疑」——现实里，多数人说错话的时候并没有恶意。 |
| 成长页雷达图下方（`legendHint` 附近） | 🧭 坚定回应不是越硬越好。清晰的边界，通常听起来是平静的。 |
| 胜利横幅（`victoryBannerHint`） | 赢了这一局，不等于现实里对方就是错的。 |
| 失败横幅（`victoryBannerHint`） | 也别急着把对方都当成操控者——先看清楚，再决定怎么回应。 |
| 结算页（校准分高时，P2） | 🧭 你既守住了边界，也没有误伤别人。这是最难的一种稳定。 |
| 结算页（校准分偏低时，P2） | 🧭 这一局有一次可能的误伤。守边界和怀疑一切之间，差别在于：先确认，再判断。 |
| 成长页校准条目（P2） | 🧭 校准 · 我可能误判过 N 次（改为"我把难听的真话当成了操控"的次数） |

> 「可能误判过 N 次」的措辞建议用**中性描述**而非自我指责，避免玩家把它读成"我又错了"，从而滑向讨好型应对。

### 4.10 三个自检问题卡（按关定制 + 三段式骨架）

> 用途：复盘结语、以及导出的 md 报告（成长页可复用同一份）。**结构一致、措辞按关**：
> 每关是三条专属问题，但必须落在同一三段式骨架上，总数恒为 3。
> 页面与导出共用同一数据源（`getRealityTransfer(level)`），因此逐字一致；不允许两端各自硬编码。

**三段式骨架（不可错位的三个槽位）**

```
🧭 三个问题，帮你分辨

1. {模式性}      这是反复出现的行为，还是一次事件？
2. {表达与选择权} 他是否剥夺了你的表达或选择？
3. {表态后的反应} 你提出边界/疑问之后，他愿意听吗？

三个都是「否」→ 先当成一次不愉快的沟通，试着先确认。
三个都是「是」→ 再考虑认真设边界。
```

| 槽位 | 判断维度 | 通用版原句 | 写作要求 |
| --- | --- | --- | --- |
| ① | 模式性 | 这是反复出现的模式，还是一次情绪化的失言？ | 必须落在"一贯 vs 这一次 / 每次 vs 偶尔"的对照上 |
| ② | 表达与选择权 | 他不让你表达，还是你们只是意见不同？ | 必须落在"针对事 vs 针对人 / 表达期待 vs 要求服从"的对照上 |
| ③ | 表态后的反应 | 你提出边界之后，他愿意听吗？ | 必须落在"给标准、共同核对 vs 继续否定、让你自责"的对照上 |

> 这三个问题的价值在于：它们都是**可观察的行为标准**，不需要玩家去猜对方的心思。而"猜心思"正是过度反应的起点。
> 这也是"按关定制"必须保留三段式骨架的原因——否则它就从"可迁移的判断方法"退化为"每关多背三条知识"。

**5 关定稿问题**（完整文案含引言与"最易误读"见 4.8）

| 关卡 | ① 模式性 | ② 表达与选择权 | ③ 表态后的反应 |
| --- | --- | --- | --- |
| 1 煤气灯操控 | 这是他一贯的说话方式，还是只有这一次？ | 是你不许查证、必须相信他，还是你们可以对着记录一起看？ | 你说了「我不太确定」之后，他是帮你把事实理清，还是让你更怀疑自己？ |
| 2 职场打压 | 这是每次汇报都出现，还是只在这件确实没做好的事上？ | 他说的是这份产出，还是在说你这个人「行不行」？ | 你提出具体疑问之后，他是给了标准，还是换了个说法继续否定？ |
| 3 亲情绑架 | 这是每次沟通都绕回来的话题，还是偶尔提一次？ | 他们是在说自己的期待，还是要求你放弃自己的选择？ | 你说出难处之后，他们是在找折中办法，还是让你觉得不答应就是不孝？ |
| 4 网络围攻 | 这是很多人同时涌上来，还是只有一两个人不同意你？ | 他是在反驳你的观点，还是在给你贴标签、贴身份？ | 你说「我理解错了」之后，他是停下，还是继续追着打？ |
| 5 偏见伪装 | 这是对所有人都一致的要求，还是只对你额外加了一条？ | 他是在说你的能力不符，还是在说你的身份「不太合适」？ | 你问「标准是什么」之后，他愿意说明，还是觉得你太敏感？ |

---

## 五、「辨别力 / 校准」指标设计方案

### 5.1 指标定义

**名称**：辨别力 / 校准（建议标识 `calibration`）
**取值**：0-100 整数，与四维同量纲
**一句话定义**：**在证据不足时，是否先确认而不是先定性。**

取值锚点（供评估模型对齐，避免模型各轮标准漂移）：

| 区间 | 含义 | 典型回应 |
| --- | --- | --- |
| 90-100 | 在信息不足时主动求证，且同时表明了立场 | "我想先确认你说的具体是哪一句，我的立场是……" |
| 60-89 | 中性表达，无贴标签、无读心 | "我可以改，但我想知道标准。" |
| 30-59 | 出现贴标签 / 读心 / 绝对化 | "你就是想让我怀疑自己。" |
| 0-29 | 攻击人格或直接关门 | "你这种人没什么好说的。" |

**重要说明**：`calibration` **衡量的是"下判断的方式"，不是"下判断的强度"**。
- 在确凿的操控面前坚定拒绝 → **不扣**校准分（此时证据充分）。
- 在证据不足时贴标签、读心、绝对化 → **扣**校准分。

### 5.2 三种落位方式对比

| 方案 | 做法 | 优点 | 代价 | 结论 |
| --- | --- | --- | --- | --- |
| **A. 扩展 `DimensionScores` 为第 5 维** | 在 `shared` 的 `DimensionScores` 加 `calibration` | 语义最完整，可进雷达图 | 牵动面最大：`utils/dimensions.ts` 的 `DIMENSION_KEYS`/`emptyDimensions`/`normalizeDimensions`/`averageOf`/`averageDimensions`/`hasAnyScore`，`gameStore` 派生与持久化，`ReviewScreen` 评分条，`reportExport`，后端 `DIMENSION_KEYS`/`toPercentScore`/`isPlaceholderDimensions`/结算公式，`SavedReviewReport` 全部要改；**四维均分口径被稀释，历史成绩与新成绩不可比** | ❌ 不推荐 |
| **B. 在 `NPCResponseAssessment` 上加独立可选字段** | 新增 `calibration?: number`、`misjudged?: boolean`、`overreaction?: "none" \| "label" \| "mindread" \| "absolutize" \| "shutoff"` | 侵入小；四维均分、`bestScores`、历史口径**完全不变**；旧存档自然兼容（字段缺失即不展示） | 需自建聚合与展示；不能进雷达图（可另做独立条） | ✅ **推荐** |
| **C. 不改结构，只调提示词口径** | 在 `ASSESSMENT_INSTRUCTION_TEMPLATE` 里明确"读心/贴标签/人身攻击式回应不得给高 `assertiveResponse`"，在 `INSIGHT_PROMPT` 里补"先确认型"策略 | 成本最低、立即可做 | 只是软约束，模型可能不遵守；玩家看不到任何反馈 | ✅ **作为 B 的兜底一并实施** |

**推荐组合：B 为主 + C 兜底**。

- 用 C 先把"越凶越有效"的口径堵住（P1，一周内可验证效果）。
- 用 B 让"校准"变成玩家看得见的反馈（P2，需前后端联动）。

### 5.3 数据流（方案 B 落地路径）

```
评估模型（chat/route.ts，与四维同一次调用，不新增 LLM 请求）
   ↓ 输出 calibration / overreaction / misjudged
后端归一化（复用 toPercentScore，DIMENSION_KEYS 不动）
   ↓ SSE assessment 事件（现成通道，只多几个字段）
前端 store（gameStore 仅透传存储，不并入 avgScore / bestScores）
   ↓
复盘页单独区块展示  →  导出 md / json 附带  →  成长页校准趋势（可选）
```

关键点：**全程不新增串行 LLM 调用**。`calibration` 与四维共用 `ASSESSMENT_INSTRUCTION_TEMPLATE` 的那一次评估请求，只多输出一个字段，对耗时与成本零影响。

### 5.4 与现有四维 / 护盾 / 控制力的协同

| 项 | 是否受校准影响 | 说明 |
| --- | --- | --- |
| `dimensions` 四维 | **间接**：当 `overreaction !== "none"` 时，对 `assertiveResponse` 封顶（建议 `min(assertiveResponse, 60)`） | 这是 C 方案的落地形式；不需要改结算公式，只需在评估结果归一化时加一行封顶 |
| `npcControlLevel` | 不变 | 结算公式 `rawControlDelta = (50 - assertiveResponse) / 5` 保持原样，封顶后自然生效 |
| `playerResistance`（护盾） | **不变**（误伤不扣护盾） | 见设计原则 P5：代价落在信息而非分数，避免教出回避型玩家 |
| `playerStatus` | **新增第 4 态 `overreacting`**（可选） | 若担心扩枚举影响面，也可不加，只用 `overreaction` 字段表达 |
| 综合分 `avgScore` | **不变**，仍为四维均值 | 校准分**不参与**综合分与"完美防御"判定，避免又一个可刷的分数目标 |
| 胜利条件 | 不变 | `effectiveStreak >= 2 && npcControlLevel < 30` 保持原样 |

> 为什么校准**不进综合分**：一旦它变成可刷的分，玩家会开始表演"我想先确认"，把辨别力训练变成话术模板背诵——这正是要避免的。校准只用于展示与反馈，是"看待自己的镜子"，不是"打怪得分"。

### 5.5 「非操控轮（真话轮）」机制设计

这是让玩家**真实体验一次误判**的机制，是整份方案里教育价值最高、也最容易做砸的一环。

**设计形态**：每关固定或随机插入 1 轮 NPC 台词，内容为**难听但有道理的实话**（不是操控）。

> 命名说明：建议叫「**非操控轮 / 真话轮**」，而不是「善意轮」。理由有两点：
> ① NPC 的人格设定是"精于心理操控的操控者"（`NPC_PERSONA_TEMPLATE`：*"你擅长使用{levelTactics}等操控手法…你表面友善、关心，但实质是在瓦解玩家的防御"*），让他在某一轮突然变善意，与人格直接冲突，模型很容易露馅或自相矛盾；
> ② "难听但真实的批评"更贴近现实中的误判场景——现实里的误判，大多是把**粗鲁的真心话**当成了操控，而不是把善意当成了操控。

**各关例句（供台词生成步骤参考）**：

| 关卡 | 非操控轮台词示例 | 为什么它不是操控 |
| --- | --- | --- |
| 1 煤气灯操控 | "项链我确实没动。但你刚才那句话真的很难听，我能听出来你已经在心里给我定罪了。" | 陈述自己的感受与事实，没有否认玩家记忆、没有反转加害者 |
| 2 职场打压 | "你报告第三页的数据是错的，我当着大家说不是要给你难堪，这个错客户会看到。" | 就事论事、指出具体位置、说明后果，无比较打压、无预言失败 |
| 3 亲情绑架 | "你不想回来我明白。但你把我们每次打电话都当成施压，我也挺难受的。" | 表达自己的感受，没有用牺牲/孝道要求玩家服从 |
| 4 网络围攻 | "你上一条确实说得不清楚，我第一遍看也误解了。" | 具体、就事论事，不贴标签、不号召围攻 |
| 5 偏见伪装 | "这岗位的硬性要求是三年经验，你不符合。这不是针对你，是规则。" | 标准明确、可验证，不要求玩家额外证明自己 |

**触发与判定**：

1. **触发**：每关指定一轮（建议 `turnCount === 2`，或按关卡配置随机 2-4 轮之一），在**台词生成步骤**（不是评估步骤）把 prompt 从"操控话术"切换为"真话轮"。
2. **当轮评估**：`trapType` 固定为「非操控（真实反馈）」，`isGenuine: true`；该轮**不参与** `npcControlLevel` 结算、不计入 `effectiveStreak`（否则玩家可以用"打赢"的方式通关这一轮，机制上就否定了它）；只结算 `calibration` 与 `overreaction`。
3. **揭晓**：仅在复盘页显示「🔎 这一轮不是操控，是对方在说真话」。
4. **防泄题**：对局中 UI 不给任何提示（不给标签、不给特殊气泡颜色）；评估结果里的 `isGenuine` 实时下发但**对局界面不渲染**，只在复盘时使用。

**与现有机制的衔接**：

- 不新增 LLM 请求：只是把某一轮的台词生成 prompt 换成分支，评估仍在同一次调用里完成。
- `turnCount` / `MAX_TURNS` 语义不变，该轮占一个正常回合。
- 兜底路径：若当轮评估走了 `degraded`（模型空返回/解析失败），则该轮既不判 `isGenuine` 也不结算校准，与现有"占位值不计入"的规则一致。

**风险**：模型可能"半个操控半个真话"，导致这一轮语义模糊。缓解办法：台词生成后做一次轻量规则检查（是否包含愧疚要挟词、是否否定玩家记忆、是否人身攻击），命中即回退为普通操控轮并跳过校准结算。

### 5.6 存档与兼容策略

| 场景 | 处理 |
| --- | --- |
| `SavedReviewReport.version` | **保持 `1`**（只新增可选字段，结构向后兼容；无需迁移） |
| 旧存档（无校准字段） | 复盘页显示"本次对局未记录校准数据"（或整块不渲染）；成长页不显示校准条目；导出 md 不输出该校准段落 |
| 新增字段是否计入统计 | **否**。`avgScore`、`bestScores`、`gameHistory` 派生一律保持四维口径 |
| 导出兼容 | `reportExport.ts` 的 `dimsToText` 不动；校准作为独立段落附加（仅当字段存在时输出） |
| `degraded` 轮 | 与四维同样的规则：兜底轮不给校准分、不做校准对比文案 |
| 新旧成绩可比性 | 完全可比（四维口径未变） |

### 5.7 徽章与成长页（P2 细节）

- 新增 `calibrator`（🧭 校准者）与 `gentle_boundary`（🕊️ 温和的边界），**不设"高分"门槛**，只设"事件门槛"（发生过一次即得），避免变成新的刷分目标。
- 成长页新增「🧭 校准」条目：显示"可能误判 N 次"，并在其下方固定展示 4.10 的三个自检问题卡。
- **不改**「📊 四维能力雷达」的结构与口径（避免破坏与历史记录的可比性）。

### 5.8 风险与反向风险控制

| 风险 | 表现 | 缓解 |
| --- | --- | --- |
| **过度校正**（滑向讨好） | 玩家学会"必须先认错才能得分" | 校准分对"道歉/自我否定"不给高分（明确写进评估提示词）；复盘的「更好的回应方式」保留坚定脚本；结论文案不出现"你太敏感了"式自我指摘 |
| **表演式确认** | 玩家固定套模板"我想先确认一下" | 校准分不进综合分、不设分数徽章；评估要求"确认之后必须表明立场"才给高分 |
| **机制过重压过内容** | 玩家为了拿"校准者"徽章去猜哪一轮是真话轮 | 真话轮比例固定且对局中不反馈、不揭晓；徽章只要求"没有误伤"而非"识别出真话轮"，猜不出也不影响 |
| **模型不稳定** | 校准分随机波动，玩家感到被冤枉 | 校准只作展示，不参与结算与胜负；兜底轮不产出校准；复盘页对低校准分轮次用中性描述（"这一句把事换成了人"），不下人格判断 |
| **叙事撕裂** | 一边教"要警惕"，一边教"别怀疑" | 全站统一措辞为「辨别」：警惕的对象是**反复出现的行为模式**，不是**每一句话** |

### 5.9 若实施：需要触及的文件清单

> 仅作范围评估，本次不修改。

**P0（纯文案/展示，最低风险）**

| 文件 | 改动 |
| --- | --- |
| `shared/src/index.ts` | `KnowledgePoint` 加 `nonSignals?`、`confirmationPrompts?`（可选字段） |
| `frontend/src/components/KnowledgeCard.tsx` | 新增两块渲染 + 5 关 `LEVEL_KNOWLEDGE` 文案 |
| `frontend/src/utils/realityTransfer.ts` | **新增**：现实迁移文案的单一来源。按关卡定制（`REALITY_BY_LEVEL` + `getRealityTransfer(level)`，钳位兜底），每关给引言、一条「最容易被误读成操控的」、三个自检问题（三段式骨架 + 恒 3 条）；标题/栏目名/结果分支/收尾句跨关共用 |
| `frontend/src/components/ReviewScreen.tsx` | 结语「🌱 把练习带回现实」（按 `level` 取本关文案）+ 三个自检问题卡；`getAdvice` default 分支文案 |
| `frontend/src/utils/reportExport.ts` | 导出 md 追加现实迁移段落（同样按 `report.level` 取本关文案，与页面共用一个数据源） |
| `frontend/src/pages/HomePage.tsx`、`frontend/src/components/GrowthScreen.tsx` | 各一句长期提示语 |

**P1（提示词口径，中风险）**

| 文件 | 改动 |
| --- | --- |
| `backend/app/api/insight/route.ts` | `INSIGHT_PROMPT` 增加 A 类「先确认」策略、配比硬约束、12 条中的 2-3 条 few-shot、禁止定性句式 |
| `backend/app/api/chat/route.ts` | `ASSESSMENT_INSTRUCTION_TEMPLATE` 增加"读心/贴标签/绝对化不得给高 `assertiveResponse`"与封顶规则；评估结果归一化处加 `Math.min(assertiveResponse, 60)` |
| `backend/app/api/review/complete/route.ts` | 增加可选字段 `misjudgmentRisk`；把 `trapAnalysis` 的措辞要求从"点破动机"改为"说明这句话在对话里的作用" |
| `frontend/src/components/BattleScreen.tsx` | 建议 chip 加类别标签；接收并展示 `overreaction` 提示语 |
| `frontend/src/components/ReviewScreen.tsx` | 渲染「🔍 这一轮可能是我误判」与「🌱 若对方是善意」；`🎯 NPC 的动机` → `🎯 这句话的作用` |
| `frontend/src/api/sse.ts` | 解析新增 SSE 字段（若沿用现有 `assessment` 事件则改动极小） |

**P2（新增指标与机制，高风险，建议单独排期）**

| 文件 | 改动 |
| --- | --- |
| `shared/src/index.ts` | `NPCResponseAssessment` 加 `calibration?` / `misjudged?` / `overreaction?` / `isGenuine?`；`ALL_BADGES` 加 2 个徽章 |
| `backend/app/api/chat/route.ts` | 评估产出新字段 + 归一化 + 真话轮分支（含规则校验回退） |
| `frontend/src/store/gameStore.ts` | 透传存储（**不得**并入 `avgScore`/`bestScores`） |
| `frontend/src/components/ReviewScreen.tsx`、`ReviewReportScreen.tsx` | 校准区块展示、真话轮揭晓 |
| `frontend/src/components/GrowthScreen.tsx` | 校准条目与趋势 |
| `frontend/src/utils/reportExport.ts` | 导出附加校准段落 |
| `frontend/src/pages/GamePage.tsx` | 校准类徽章解锁判定 |

---

## 六、实施优先级与工作量分级

| 档位 | 内容 | 风险 | 改动面 | 预期效果 | 建议 |
| --- | --- | --- | --- | --- | --- |
| **P0** | 课前卡两块对照栏（4.1 / 4.2）、复盘结语（4.8）、自检问题卡（4.10）、成长/首页提示语（4.9 静态部分） | 极低（纯展示，不碰评分） | 5 个文件，约 1 天 | 立刻给玩家"孔径边界"，成本最低、收益最直接 | **必做** |
| **P1** | 明辨铃「先确认」策略与配比（4.3 / 4.4）、评估口径封顶、复盘「可能误判」（4.6）与「若对方是善意」（4.7）由模型生成、`trapAnalysis` 去人格化、对战实时提示（4.5） | 中（会改变四维评分分布与玩家行为） | 6 个文件，约 2-3 天（含 prompt 调优与抽样验证） | 把"先定性"的条件反射打断，让"非对抗路径"成为常规选项 | **推荐做** |
| **P2** | `calibration` 指标、真话轮机制、校准徽章、成长页校准条目、导出扩展 | 高（改评估结构、涉及 LLM 稳定性与体验风险） | 8 个文件，约 1 周（含真话轮的模型行为验证） | 让"我可能误判了"成为可被体验、被记住的反馈 | **建议先小范围试跑 1 关再全量** |

**分批实施建议**：

1. **第一周**：只上 P0，观察玩家对"这些不算操控"的反馈（是否有玩家在复盘里主动提及）。
2. **第二周**：上 P1，重点验证三件事——明辨铃 A 类建议的采纳率、`assertiveResponse` 分布是否下降（预期会降，属正常）、玩家是否仍能通关。
3. **第三周起**：在**单关（建议第 2 关职场打压，真话轮语义最清晰）**试跑 P2 的真话轮 + 校准分，验证模型的"真话/操控"区分稳定性，再决定是否推广到 5 关。

---

## 七、验收与自检清单

### 7.1 效果验收（定性）

抽取 10 名通关玩家的复盘，检查是否出现以下变化：

- [ ] 玩家能主动说出"什么时候**不该**用操控框架"（而不是只会复述 5 种操控手法）
- [ ] 玩家在复盘里对"可能误判"段落的停留时间与引用次数 > 0
- [ ] 玩家对"如果对方是善意"脚本的评价是"有道理"而非"多余的免责声明"

### 7.2 效果验收（量化）

| 指标 | 现状基线 | 目标 |
| --- | --- | --- |
| 明辨铃建议中 A 类（先确认）采纳率 | 0%（当前话术池无 A 类） | ≥ 20% |
| 玩家回应中"贴标签/读心/绝对化"占比 | 需先埋点统计 | 下降 ≥ 30%（相对值） |
| 真话轮的误伤率（P2） | — | 第 1 次接触时 ≤ 60%，第 2 关起 ≤ 40% |
| 三个自检问题的正确复述率 | — | ≥ 70% |
| `assertiveResponse` 均值 | 需埋点 | 允许下降（这是预期效果，不是退化） |

### 7.3 工程自检

- [ ] 四维口径未变（历史成绩可比）
- [ ] `avgScore`、`bestScores`、`perfect_defense` 判定未变
- [ ] 旧存档（`version = 1`，无校准字段）能正常打开复盘与导出
- [ ] `degraded` 兜底轮不产出校准分、不生成校准对比文案
- [ ] 明辨铃新话术全部通过 `cleanAlternativeText` 清洗（第一人称、无引导词、≥ 3 字）
- [ ] 全站措辞统一为「辨别」，未出现"你太敏感了"式自我指摘
- [ ] `chat/route.ts` 日志补上 `calibration=` / `overreaction=` / `isGenuine=` 字段（与现有 assessment 日志同格式，便于线上观测）

---

## 附录 A：文案语域规范

写新增文案时统一遵守，避免与游戏现有风格割裂：

1. **口吻**：温柔克制的心理科普口吻，不用"教材腔"（禁止"综上所述""由此可见"），不用营销腔（禁止"轻松三步搞定"）。
2. **人称**：面向玩家的建议一律第一人称原话（"我想先确认…"）；面向玩家的解释用"你"，不用"玩家"。
3. **emoji**：沿用现有习惯 —— 📚 课前导读 / 🔍 识别信号 / ⚠️ 反例 / ⏸ 先确认 / 💬 健康应对 / 📝 真实案例 / 🎯 这句话的作用 / 🌱 现实迁移 / 🧭 校准。不新造风格不一致的符号。
4. **长度**：卡片条目 ≤ 30 字；对话话术 10-40 字；点评段落 20-60 字（与现有 `ASSESSMENT_INSTRUCTION_TEMPLATE` 的"每段 20~60 汉字"一致）。
5. **禁用句式**：
   - 对 NPC 定性："他就是想操控你""这个人不爱你" → 改为描述行为的作用。
   - 对玩家定性："你太敏感了""你又过度反应了" → 改为描述这句话做了什么。
   - 绝对化："永远""从来""一定"（无论对 NPC 还是对玩家）。
6. **必须成对出现**：任何"识别信号"都要有对应反例；任何"更好的回应方式"都要有对应的"若对方是善意"脚本。

## 附录 B：常见疑问

**Q1：加了"这些不算操控"，会不会削弱游戏的教育效果，让玩家不敢识别操控？**
不会。辨别力是"识别 + 排除"两项能力，只教识别训练出的是一半能力。真实案例已经说明这一点：第 2 关的 `caseStory` 原文写的是"他后来学会**区分**「批评」与「打压」"——产品自己的案例就在讲区分，但卡片结构里没有这一栏。

**Q2：为什么校准分不进综合分？玩家不会觉得它不重要吗？**
它确实"不重要"——这是刻意的。它一旦可刷，就会变成话术模板背诵。它的作用是在复盘时当镜子，而不是在结算时当分数。徽章用"事件门槛"（发生过一次）而非"分数门槛"，就是为了让它奖励"发生过辨别"，而不是"刷出高分"。

**Q3：真话轮会不会让玩家觉得被骗？**
会有一点，所以揭晓时机很重要：**对局中绝不提示，复盘时才温和地说明**。措辞上不说"你被骗了"，而是"这一轮其实不是操控——很多误判都发生在类似的地方"。把"被骗感"转化为"原来这里很容易读错"，这是这个机制的完整教育闭环。

**Q4：是否可以只做 P0？**
可以，而且建议先只做 P0。P0 是纯展示层，零风险，且直接回应了用户的核心担忧；P1/P2 的效果依赖 prompt 调优与模型稳定性验证，适合在第一周的玩家反馈之后再决定投入。

---

## 附录 C：已落地的改动（四维结算口径 / "血量"命名统一 / 心域护盾资源化 / 评估重试链路）

> 这一节记录的是**已落地的改动**，不属于"待评审的方案"，放在附录以免与正文的优先级分级混淆。

### C.1 问题：四维里有两维是"空转"的

改动前的结算只用了两个维度：

| 通道 | 旧公式 | 决定者 |
| --- | --- | --- |
| 抵抗值损耗 | `10 − 边界意识/10` | 边界意识 |
| NPC 控制力变化 | `clamp((50 − 坚定回应)/5, ±15)` | 坚定回应 |

`情绪稳定` 与 `认知清晰` 在后端只出现在三处——提示词模板、中文名映射、维度校验兜底——**结算里完全没用到**，只影响记录与综合分，玩家给它们打高分不会有任何战局反馈。

更糟的是提示词里写的是：

> "dimensions 沿用旧逻辑：坚定回应（assertiveResponse）主导控制力结算，**其余维度影响护盾/迷雾**"

这与实现不符（没有任何维度通过"护盾"通路起作用），是一处会误导后续维护者的错误描述。

### C.2 改法：双维加权（主维 0.6 + 辅维 0.4）

```
resistDelta  = 10 − (0.6 × 边界意识 + 0.4 × 情绪稳定) / 10
            ↓ 抵抗值损耗：情绪一慌，护盾掉得更快
controlDelta = clamp((50 − (0.6 × 坚定回应 + 0.4 × 认知清晰)) / 5, ±15)
            ↓ 控制力削减：看得越清，砍得越狠
```

**为什么取 0.6 / 0.4**
- 主维保持主导，与 UI 标签层级、提示词口径一致；
- 副维提供分辨度：认知清晰从"完全无效"变成约 **0.08 点控制力/分**（每 10 分约 ±0.8）。

**为什么这个改动是安全的**

| 组合 | 抵抗值/轮 | 控制力/轮 | 与旧公式相比 |
| --- | --- | --- | --- |
| 四维全 100 | −0 | −10 | **完全一致** |
| 四维全 0 | −10 | +10 | **完全一致** |
| 四维全 50（中位玩家） | −5 | 0 | **完全一致** |

极值与中位数行为都不变，因此各关初始值 `LEVEL_STATS`（抵抗 100/90/80/70/60、控制 50/55/60/65/70）**无需重新平衡**；变化的只是"偏科玩家"的收益：认知高、坚定低的玩家现在能砍动控制力了。

**配套改动**
- 提示词那句错误描述已改写为新口径，并加了"四个维度都会实际影响战局，不要四维同分"的强约束（此前模型容易给一排 50，而 50 恰好是"无贡献"的中性值）。
- 回合结算浮层给两行各加一条归因小字（`由 坚定 80 · 认知 80 加权得出`），否则玩家只会看到数字跳动，无法感知这两维在起作用。归因用前端已有的 `dims` 本地计算，**不改 SSE 协议**。
- 后端日志补 `结算等效分 边界=X 坚定=Y`，用于线上排查"某维是否真的参与了结算"。
- 注入提示词前对 `playerResistance` 取整：加权后该值必为小数（如 92.19999999），避免模型读到浮点噪声。

**关于迷雾**：迷雾已无实际作用，因此本次**没有**把任何维度的效果挂到迷雾上，相关代码与状态一律保持原样、不作为新效果的载体。

### C.3 命名统一：三个名字曾经指两个数

| 数值 | 存储 | 旧界面标签 | 初始值 | 参与结算 |
| --- | --- | --- | --- | --- |
| 心域护盾（元层面） | `sanctuary.shieldHealth` | 备战页「护盾强度」、修复页「护盾恢复至」、NPC 提示词「护盾强度」 | `max(60, 100−(关−1)×10)` | ❌ |
| 本场抵抗值 | `conversation.playerResistance` | 对战页「抵抗值」、结算浮层「**心域护盾**」 | `100/90/80/70/60` | ✅ |

三个问题：① 同一个数值在对战页叫「抵抗值」、在结算浮层叫「心域护盾」；② 备战页的"护盾强度"与对战页的"抵抗值"初始序列恰好都是 100/90/80/70/60，看起来像同一个数，实际是两个独立变量；③ NPC 提示词同时注入「护盾强度：{shieldHealth}」（不参战）与「玩家抵抗值：{playerResistance}」，同一概念两个数，模型容易说出与实际战局不符的台词。

**落地口径**

| 位置 | 改为 |
| --- | --- |
| 结算浮层 | 「心域护盾」→「**抵抗值**」（与对战页血条同名） |
| 备战页 | 「护盾强度」→「**心域护盾**」+ 小字"心域护盾是上一场之后的心域状态，用于战后修复；对抗中的血量见对战页的「抵抗值」" |
| 修复页「护盾恢复至」 | 不变（它确实在改 `shieldHealth`） |
| NPC 提示词 | 删掉「护盾强度」那一行，只保留「玩家抵抗值」 |

**刻意不改**：字段名 `shieldHealth` / `playerResistance`、SSE 事件名 `shield_damage`、store 方法名、存档结构——协议与存档零风险。

### C.4 心域护盾：从"装饰数值"变成真实资源

C.3 只解决了命名，但那时的诊断是：**心域护盾是一个没有后果的孤儿数值**（不参战、可视化被注释、`HeartDomainMini` 组件无人引用、`setShieldHealth` 只在修复页被调用）。本节把它接进玩法。

**机制改动**

| 环节 | 行为 |
| --- | --- |
| 初始值 | 固定 `100`（**不再**随关卡递减为 100/90/80/70/60） |
| 每局结束（写入战绩时） | **按胜负磨损**：胜利 `−5`、失败 `−25`，下限 `40` |
| 修复阶段 | `min(100, 当前 + 边界完整性 × 0.2)`（最多 `+20`） |
| 点「下一关」 | **跨关保留**，磨损累积 |
| 失败后「重试」/「再等等」 | **保留**（一次重开不该清零心域） |
| **回到首页（任意入口）** | **复位**：护盾养满、关卡回第 1 关 |
| 作用 | 开局抵抗值 = `LEVEL_STATS[关].resistance × (1 − 0.5 × (100 − 护盾)/100)` |

**复位点为什么放在"首页挂载"而不是"返回首页按钮"**

回到首页有多条路径：游戏页的「返回首页」按钮、成长记录页的 `history.back()`（`App.tsx:20`）、地址栏直开 `/`。原先只挂在按钮上，于是"游戏页返回"会复位、"从成长记录返回"不复位——同一个首页两种心域状态。现在统一为一句规则：**到达首页即复位**（`HomePage` 挂载时执行 `resetLevel()`）。

**由此确定的规则**

- 回到首页 → 护盾养满；
- 首页选关 → 必然带着满护盾进去，**开局抵抗值就是该关基准值**（100/90/80/70/60）；
- **护盾的下降只由"失败"造成**：胜利磨损 `5`、修复一次 `+16~20`，必定回满；失败磨损 `25` 而修复最多 `+20`，净降约 `6` 点，只有连败才会明显吃力。

**磨损与修复的配比（关键约束，改任一侧都要重新核算）**

修复加成上限 = 完整性 `100 × 0.2 = 20`，这个上限**必须小于失败磨损 `25`**，否则修复会把失败完全抹平，护盾将恒定 100、机制失效——最初版本正是踩了这个坑：固定磨损 `−10` 配 `× 0.5` 修复（最多 `+50`），而修复是每局必经流程，于是每打一局都被补回满，机制等于没做。

| 场景 | 护盾变化 | 下一关系数 |
| --- | --- | --- |
| 正常通关（胜） | `100 − 5 = 95` → 修复 `+16~20` → **封顶 100** | `1.000`（与旧版基准一致） |
| 失败一次 | `100 − 25 = 75` → 修复 `+18.8` → **93.8** | `0.969` |
| 连败两次 | `93.8 − 25 → 68.8` → `+18.8` → **87.6** | `0.938` |
| 连败后赢一局 | `87.6 − 5 = 82.6` → `+18.8` → **封顶 100** | `1.000` |
| 持续失败到下限 | 每次净降约 `6`，约 10 次触底 `40` | `0.700`（最差 −30%） |

**为什么磨损要按胜负区分**：固定磨损（无论输赢都扣同样的值）看似公平，但在"每局必经修复"的流程下会被一次修复补回，机制失效。按胜负区分后，**赢了必定回满**（不惩罚正常推进）、**只有失败才真正打薄心域**（连败越来越吃力），语义上也更贴合"心域是被击穿才受损"。

**养护不会形成死循环**：失败后重试同一关时护盾保留，但只要赢下一局，修复就能把护盾一次养回满——所以它是"连败的压力"，不是"越输越难爬的惩罚"。

**为什么这样设计不会产生套利**

护盾只做"惩罚"、不做"加成"（上限恒为 100，满值就等于基准难度）。因此任何复位路径最多把护盾恢复到 100，**不存在任何超额收益**，也就没有"刷心域"的动机。

需要说明的是：既然"回首页即复位"，玩家确实可以通过"先回首页再选关"避开累积磨损。但这不构成漏洞——它只是把难度拉回**基准**（拿不到任何优势），而且本产品是教育向、无排行榜，没有靠难度牟利的动机。若日后要收紧，做法是让「首页选关」也计一次磨损（每次进入关卡都侵蚀一次），代价是"随便进去看看"也会损失心域，体验更重。

**并修掉的反直觉点**：原来 `resetForLevel` / `setCurrentLevel` 会把 `shieldHealth` 重置为本关公式值，导致"修复成果在回首页或重新选关时消失"。现在两处都改为保留当前护盾（只重置对话、迷雾、法器冷却与装备）。

**已知取舍**：`sanctuary` 从未持久化（localStorage 只存战绩、徽章、已掌握知识点、最高分），因此**重开应用后护盾回到 100**。这与旧行为一致（旧版也会回到公式值），如需跨会话保留，需要把 `shieldHealth` 加入 `PersistedData`——属于独立改动，未做。

### C.5 评估失败的重试链路（含"降级前重试"）

**为什么要写这一节**：`degraded`（评估不可信）会让该轮**不计成绩**、复盘中显示"本轮评估未获取到有效数据"。它是最后一道兜底，但此前有四类失败会直接落到它身上——其中一部分其实是"模型评估了、只是格式或字段没对上"，本该救得回来。这一节把完整链路固定下来，避免以后被误认为"已经重试过了"。

**五层链路（由外到内，越靠内越靠近兜底）**

| 层 | 触发条件 | 行为 | 超时 / 预算 |
| --- | --- | --- | --- |
| ① 候选模型降级 | 请求异常或非 200 | 评估专用非推理模型（关 thinking）→ 主模型 → 备用模型；**每候选只 1 次**（HTTP 400 时切回非 `json_object` 模式再发 1 次） | 30s / 次 |
| ② 内容过短重试 | `status=200` 但内容 < 30 字符 | 追加"禁止思考"指令，再遍历三个候选各 1 次 | 30s / 次 |
| ③ 解析就地抢救（非重试） | JSON 解析失败 | 多层修复链：直接解析 → 去非法转义 → 全角冒号 → 引号修复 → 迭代截断补全 → 去末尾残缺字段 → 剥离 `alternatives` → 反向遍历所有 `{…}` 块 | — |
| ④ **降级前重试** | ① 解析彻底失败　② 四维字段缺失/非法　③ 四维为占位值（全等且为 0 或 50） | 追加"上次输出不可用，请重新输出完整 JSON"，**遍历三个候选各 1 次**；成功则覆盖结果并把 `degraded` 置回 `false` | **15s / 次**；整回合最多 1 次；回合尾部 > 45s 放弃 |
| ⑤ 本地兜底 | 以上全部失败 | `degraded: true` + 四维补 50 + 从原文正则抢救文本字段；前端不展示数值、不计成绩 | — |

对应实现：`backend/app/api/chat/route.ts` 的 `evalCandidates` / `requestWithRetry` / `retryBeforeDegrade`，常量 `ASSESS_TIMEOUT_MS` / `ASSESS_RETRY_TIMEOUT_MS` / `TURN_LATENCY_BUDGET_MS`。

**三条必须遵守的设计约束（改这块代码前先读）**

1. **只在降级路径触发**：④ 的三个调用点都在"已判定要走兜底"的分支内。**正常回合一次额外调用都没有**——零延迟、零成本。
2. **重试预算全局唯一**：整回合最多 1 次，避免"解析重试 + 字段重试 + 占位重试"串联成 3–4 次调用，把玩家等待拖到一分钟以上。
3. **不把坏输出回喂模型**：只追加一句说明，**不把上一次的畸形 JSON 塞回上下文**（会强化错误格式）——与 `review/complete` 的做法一致。

**本次修掉的三个既有缺陷**

| 缺陷 | 后果 | 修法 |
| --- | --- | --- |
| 解析失败的重试用 `doRequest(true, undefined, …)` | 落到**主模型且没关思考**，配置比首轮（评估专用非推理模型）更弱，最容易复现同类失败 | 统一走 `evalCandidates`（含关思考的非推理模型） |
| 重试成功后，结果**立刻被下面的兜底对象无条件覆盖** | 白等 30s 仍然降级，表现为"加了重试但完全没用" | 兜底前再判一次 `if (!assessment)` |
| 占位评分重试只试 `evalCandidates.slice(0, 1)` | 首选评估模型排队/超时即认输 | 统一走 `retryBeforeDegrade`，依次尝试主模型与备用模型 |

**观测口径（排查时先看这几行日志）**

| 日志 | 含义 |
| --- | --- |
| `[chat] 降级前重试（<原因>）：依次尝试评估候选模型` | 已触发重试 |
| `[chat] 降级前重试成功（<模型>）原因=… 四维={…}` | 救回来了（前端不应再出现"未获取到有效数据"） |
| `[chat] 降级前重试全部候选均失败：…，转本地兜底` | 真失败，正常走兜底 |
| `[chat] 降级前重试跳过（本回合已用过重试预算）` | 预算刹车生效 |
| `[chat] 降级前重试跳过（回合尾部已耗时 …ms，超出 45000ms 预算）` | 耗时刹车生效 |

**边界与取舍**

- **不能保证降级归零**：网络全挂、三个候选全部超时时无处可退，兜底仍必须保留。目标是把它压到"真故障才会出现"。
- **前端零改动**：`degraded` 的语义未变（不可救时才标记），复盘口径与"不计成绩"的规则都保持原样。
- **若要把预算放宽**：把 `retried` 由布尔改成计数器（上限 2）即可，代价是最坏等待从 ~15s 变成 ~30s。建议先按 1 次上线，看"全部候选均失败"的出现频率再决定。

**同一请求里其它两段的重试（对照参考）**

| 段 | 策略 |
| --- | --- |
| 台词段（第一段，流式） | 主模型 → 备用模型，**每候选最多 2 次**，单次超时 45s，成功门槛内容 ≥ 10 字符；全部失败才推 `error` |
| 复盘文本补全（`review/complete`） | 解析失败**自动重试一次**；仍失败返回错误，前端该轮显示"点此重试" |
| 明辨铃（`insight`） | 首轮清洗后无可用建议 → 追加"禁止引导词/必须第一人称"说明**重试一次**；仍失败推 `error`，前端提示并把法器冷却归零，可立刻再点 |

---

*正文部分为设计方案，按 P0 → P1 → P2 分批实施；附录 C 记录的是同期已落地的改动。*
